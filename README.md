# Practice PR workflowlanding-zone

landing zone for monolihitc application with best practices like root and child modle , 
least RBAC role assigned .
backend creation
key vault
zone redundancy
code always on github bitbucket gitlab aws commit azure repo

commond to run fot git 
git clone
git checkout -b feature/new-resource-added
git switch -c featture/new-resource-added
do chnage
git status
git add .
git commit -m "new resouce added"
git push -u origin /feature/new-resource-added